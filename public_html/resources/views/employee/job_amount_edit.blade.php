{{-- <x-auth-validation-errors class="mb-4" :errors="$errors" />

<form method="post" action="{{ route('useradmin.emp.job.amount.update') }}" enctype="multipart/form-data">
    @csrf
    <!-- Hidden input field for the ID of the employee -->
    <input type="hidden" name="id" value="{{ $details->id }}">
    <!-- Hidden input field for the user who created the record -->
    <input type="hidden" name="created_by" value="admin">

    <div class="form-group mb-3">
        <label class="form-label" for="name">{{ __('Employee Name') }}</label>
        <!-- Input field for the employee name -->
        <x-input id="name" name="name" class="form-control" type="text" value="{{ $details->name }}" required autofocus />
    </div>

    <div class="form-group mb-3">
        <label class="form-label" for="job_count">{{ __('Job Count') }}</label>
        <!-- Input field for the job count -->
        <x-input id="job_count" name="job_count" class="form-control" type="number" min="1" step="1" value="{{ $details->job_count }}" required autofocus />
    </div>

    <div class="d-flex mb-3">
        <div class="d-grid">
            <!-- Submit button for updating salary -->
            <button class="btn btn-primary btn-block mt-2" type="submit">{{ __('Update Salary') }}</button>
        </div>
    </div>
</form> --}}
